<%= form_with(model: [notable, Note.new], local: true) do |form |%>
  <div class="field">
    <%= form.label :content, "Loan Amount:" %>
    <br>
    <%= form.text_field :content %>
  </div>

<br>

    <!-- <%= form.submit class:"btn btn-primary" %> -->
<% end %>
